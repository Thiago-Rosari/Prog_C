#include <stdio.h>     // Standard Input Output
#include <stdlib.h>    // Standard Library

// função principal
int main(void)
{
    // comentários de uma linha iniciam com // (duas barras)

    // A função printf direciona o texto inserido (entre aspas duplas) para a saída padrão
    printf("Hello, World!");

    return 0;
}